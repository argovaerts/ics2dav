services:
  ics2dav:
    restart: unless-stopped
    image: argovaerts/ics2dav:latest
    ports: []
    environment:
      - ICS_URL=
      - CALDAV_URL=
      - USERNAME=
      - PASSWORD=
networks: {}